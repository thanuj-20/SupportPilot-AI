# torch/torchaudio installed by render-build.sh via CPU wheel index
# DO NOT add torch here — pip would pull CUDA build from PyPI

# ── Core API ───────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0
python-multipart==0.0.9
python-dotenv==1.0.0

# ── Database ───────────────────────────────────────────────────────────────
pymongo==4.7.2
motor==3.4.0

# ── ML ─────────────────────────────────────────────────────────────────────
scikit-learn==1.9.0
joblib==1.4.2
pandas==2.2.3
openpyxl==3.1.2
numpy==1.26.4

# ── Embeddings + FAISS (torch already installed before this runs) ──────────
huggingface-hub==0.22.2
tokenizers==0.15.2
transformers==4.39.3
sentence-transformers==2.7.0
faiss-cpu==1.7.4

# ── Document parsing ───────────────────────────────────────────────────────
pymupdf==1.23.26
python-docx==1.1.2
